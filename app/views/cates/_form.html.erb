<%= form_for(@cate) do |f| %>
  <% if @cate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cate.errors.count, "error") %> prohibited this cate from being saved:</h2>

      <ul>
      <% @cate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br />
    <%= f.number_field :parent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
